{
  "name": "yt-twitch-chat-toggle",
  "version": "0.0.0",
  "devDependencies": {
    "@types/chrome": "^0.1.6",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  },
  "private": true,
  "scripts": {
    "build:main": "bun run format &&tsc -b && bunx --bun vite build --config vite.config.ts",
    "build:content": "bunx --bun vite build --config vite.config.content.ts",
    "build": "bun run build:main && bun run build:content",
    "format": "bunx --bun prettier --write ."
  },
  "type": "module",
  "dependencies": {
    "@types/bun": "^1.2.23"
  }
}
