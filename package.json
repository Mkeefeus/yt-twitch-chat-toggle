{
  "name": "yt-twitch-chat-toggle",
  "version": "0.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/chrome": "^0.1.6",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  },
  "private": true,
  "scripts": {
    "build:chrome": "bunx --bun vite build --config vite.config.chrome.ts && bunx --bun vite build --config vite.config.content.chrome.ts && bun run manifest:chrome",
    "build:firefox": "bunx --bun vite build --config vite.config.firefox.ts && bunx --bun vite build --config vite.config.content.firefox.ts && bun run manifest:firefox",
    "manifest:firefox": "bun run scripts/generate-manifest firefox",
    "manifest:chrome": "bun run scripts/generate-manifest chrome",
    "manifest": "bun run scripts/generate-manifest",
    "type-check": "tsc -b",
    "format": "bunx --bun prettier --write .",
    "build": "bun run type-check && bun run format && bun run build:chrome && bun run build:firefox && bun run manifest"
  },
  "type": "module"
}
